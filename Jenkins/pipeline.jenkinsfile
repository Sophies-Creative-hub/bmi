pipeline {
    agent any
    stages {
        stage('Checkout') {
            steps {
                // Das Projekt-Repository aus Git abrufen
                git url: 'https://github.com/dein-benutzer/dein-projekt.git'
            }
        }
        stage('Installieren') {
            steps {
                // Node.js und npm installieren
                sh 'curl -sL https://deb.nodesource.com/setup_18.x | bash -'
                sh 'apt-get install -y nodejs'
                sh 'npm install'
                sh 'npx cypress install'
            }
        }
        stage('Cypress-Tests') {
            steps {
                sh 'npm test' // Führe Cypress-Tests aus
            }
        }
        stage('Aufzeichnen der Cypress-Testergebnisse') {
            steps {
                sh 'npx cypress run --record --key <CYPRESS_RECORD_KEY>'
            }
        }
    }
}
